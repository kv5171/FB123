<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ex123">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ex123">
        <activity android:name=".ChangePhoneActivity"></activity>
        <activity android:name=".SortActivity" />
        <activity android:name=".CreditsActivity" />
        <activity android:name=".ShowAllActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".CompanyActivity" />
        <activity android:name=".EmployeeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>